{{#if bus}}
<h2>{{bus.fields.nomcourtligne}}</h2>

<dl>
    <dt>Destination</dt>
    <dd>{{bus.fields.destination}}</dd>
    <dt>Écart</dt>
    <dd>{{bus.fields.ecartsecondes}}s (il y a {{since}}{{unit}})</dd>
</dl>
{{/if}}
